1967. Number of Strings That Appear as Substrings in Word (Easy)


Hint
Given an array of strings patterns and a string word, return the number of strings in patterns that exist as a substring in word.

A substring is a contiguous sequence of characters within a string.

 

Example 1:

Input: patterns = ["a","abc","bc","d"], word = "abc"
Output: 3
Explanation:
- "a" appears as a substring in "abc".
- "abc" appears as a substring in "abc".
- "bc" appears as a substring in "abc".
- "d" does not appear as a substring in "abc".
3 of the strings in patterns appear as a substring in word.
Example 2:

Input: patterns = ["a","b","c"], word = "aaaaabbbbb"
Output: 2
Explanation:
- "a" appears as a substring in "aaaaabbbbb".
- "b" appears as a substring in "aaaaabbbbb".
- "c" does not appear as a substring in "aaaaabbbbb".
2 of the strings in patterns appear as a substring in word.
Example 3:

Input: patterns = ["a","a","a"], word = "ab"
Output: 3
Explanation: Each of the patterns appears as a substring in word "ab".
 

Constraints:

1 <= patterns.length <= 100
1 <= patterns[i].length <= 100
1 <= word.length <= 100
patterns[i] and word consist of lowercase English letters


Test Case:
Input
patterns = ["a","abc","bc","d"]
word = "abc"
Output = 3

Input
patterns = ["a","b","c"]
word = "aaaaabbbbb"
Output = 2

Answer:
from typing import List

class Solution:
    def numOfStrings(self, patterns: List[str], word: str) -> int:
        ans = 0

        for pattern in patterns:
            if pattern in word:
                ans += 1

        return ans


Time Complexity: 

Let:

n = len(patterns)
m = len(word)
k = average length of a pattern

Checking

pattern in word

takes approximately O(m × k) in the worst case.

Overall: O(n × m × k)


Explanation

We need to count how many strings in patterns appear as a substring of word.

Python provides the in operator for strings, which checks whether one string is a substring of another.

For every pattern:

If pattern in word is True, increase the count.
Otherwise, ignore it.

Finally, return the count.

Example 1
patterns = ["a","abc","bc","d"]
word = "abc"

Check each pattern:

Pattern	In "abc"?	Count
"a"	✅	1
"abc"	✅	2
"bc"	✅	3
"d"	❌	3

Answer:

3
Example 2
patterns = ["a","b","c"]
word = "aaaaabbbbb"
"a" ✅
"b" ✅
"c" ❌

Answer:

2
Example 3
patterns = ["a","a","a"]
word = "ab"

Each "a" is checked separately.

Since "a" is a substring of "ab":

Count = 3
