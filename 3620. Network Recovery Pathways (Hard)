3620. Network Recovery Pathways (Hard)



Hint
You are given a directed acyclic graph of n nodes numbered from 0 to n − 1. This is represented by a 2D array edges of length m, where edges[i] = [ui, vi, costi] indicates a one‑way communication from node ui to node vi with a recovery cost of costi.

Some nodes may be offline. You are given a boolean array online where online[i] = true means node i is online. Nodes 0 and n − 1 are always online.

A path from 0 to n − 1 is valid if:

All intermediate nodes on the path are online.
The total recovery cost of all edges on the path does not exceed k.
For each valid path, define its score as the minimum edge‑cost along that path.

Return the maximum path score (i.e., the largest minimum-edge cost) among all valid paths. If no valid path exists, return -1.

 

Example 1:

Input: edges = [[0,1,5],[1,3,10],[0,2,3],[2,3,4]], online = [true,true,true,true], k = 10

Output: 3

Explanation:



The graph has two possible routes from node 0 to node 3:

Path 0 → 1 → 3

Total cost = 5 + 10 = 15, which exceeds k (15 > 10), so this path is invalid.

Path 0 → 2 → 3

Total cost = 3 + 4 = 7 <= k, so this path is valid.

The minimum edge‐cost along this path is min(3, 4) = 3.

There are no other valid paths. Hence, the maximum among all valid path‐scores is 3.

Example 2:

Input: edges = [[0,1,7],[1,4,5],[0,2,6],[2,3,6],[3,4,2],[2,4,6]], online = [true,true,true,false,true], k = 12

Output: 6

Explanation:



Node 3 is offline, so any path passing through 3 is invalid.

Consider the remaining routes from 0 to 4:

Path 0 → 1 → 4

Total cost = 7 + 5 = 12 <= k, so this path is valid.

The minimum edge‐cost along this path is min(7, 5) = 5.

Path 0 → 2 → 3 → 4

Node 3 is offline, so this path is invalid regardless of cost.

Path 0 → 2 → 4

Total cost = 6 + 6 = 12 <= k, so this path is valid.

The minimum edge‐cost along this path is min(6, 6) = 6.

Among the two valid paths, their scores are 5 and 6. Therefore, the answer is 6.

 

Constraints:

n == online.length
2 <= n <= 5 * 10^4
0 <= m == edges.length <= min(105, n * (n - 1) / 2)
edges[i] = [ui, vi, costi]
0 <= ui, vi < n
ui != vi
0 <= costi <= 10^9
0 <= k <= 5 * 10^13
online[i] is either true or false, and both online[0] and online[n − 1] are true.
The given graph is a directed acyclic graph.



Test Case:

Input

edges = [[0,1,5],[1,3,10],[0,2,3],[2,3,4]]
online = [true,true,true,true]
k = 10
Output = 3


Answer:
class Solution:
    def findMaxPathScore(self, edges: List[List[int]], online: List[bool], k: int) -> int:
        n = len(online)

        graph = [[] for _ in range(n)]
        indeg = [0] * n
        vals = []

        for u, v, w in edges:
            graph[u].append((v, w))
            indeg[v] += 1
            vals.append(w)

        # Topological order
        q = deque(i for i in range(n) if indeg[i] == 0)
        topo = []

        while q:
            u = q.popleft()
            topo.append(u)
            for v, _ in graph[u]:
                indeg[v] -= 1
                if indeg[v] == 0:
                    q.append(v)

        vals = sorted(set(vals))

        def check(x):
            INF = float("inf")
            dp = [INF] * n
            dp[0] = 0

            for u in topo:
                if dp[u] == INF:
                    continue

                for v, w in graph[u]:
                    if w < x:
                        continue

                    if v != n - 1 and not online[v]:
                        continue

                    if dp[u] + w < dp[v]:
                        dp[v] = dp[u] + w

            return dp[-1] <= k

        ans = -1
        l, r = 0, len(vals) - 1

        while l <= r:
            mid = (l + r) // 2
            if check(vals[mid]):
                ans = vals[mid]
                l = mid + 1
            else:
                r = mid - 1

        return ans



Time Complexity: O((n + m) log m)


Explanation
Step 1: Binary Search on the Answer

Suppose the answer is X.

Can we find a path

from 0 to n-1
every edge has cost >= X
total cost ≤ k
intermediate nodes are online?

If yes, then X is possible.

If X is possible, every smaller value is also possible.

So the property is monotonic, allowing binary search.

Step 2: Check(X)

Ignore every edge whose cost is less than X.

Now we only need to know:

Is there a path whose total cost ≤ k?

Since the graph is a DAG, shortest path can be computed in one topological DP instead of Dijkstra.

Why DP works?

For every node,

dp[u] = minimum total cost to reach u

Initially

dp[0] = 0
others = INF

Traverse nodes in topological order.

For every edge

u -> v (cost)

If

edge cost ≥ X
v is online (unless it's destination)

then

dp[v] = min(dp[v], dp[u] + cost)

Finally

dp[n-1] <= k

means answer exists.

Step 3: Topological Order

Since graph is already a DAG,

Compute indegrees.

Run Kahn's Algorithm.

queue

while queue:
    u
    for v:
        indegree[v]--

This gives the topological order in

O(n+m)

We compute it once.

Example
0 --5-->1
 \      |
 3      10
  \     |
   ->2--4->3

Suppose binary search asks

X=4

Allowed edges are

5
10
4

Edge

0->2 (3)

is removed.

No path exists.

So

4 impossible

Suppose

X=3

All edges remain.

DP

dp[0]=0

0->1 =5

0->2 =3

2->3 =7

1->3 =15

Minimum

dp[3]=7

Since

7<=k

possible.

Hence answer is at least 3.

Complexity

Let

n = 5e4
m = 1e5

Topological sort

O(n+m)

Binary search over edge costs

There are at most m distinct edge costs (or log2(10^9) ≈ 31 iterations if searching the numeric range).

Each check

O(n+m)

Overall:

Using numeric binary search: O((n + m) · log 10^9) ≈ O(31(n + m))
Using binary search on the sorted distinct edge costs: O((n + m) · log m)

Both easily fit the constraints.
